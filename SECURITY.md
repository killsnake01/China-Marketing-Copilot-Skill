# 安全说明

本技能包面向中国3C上市营销决策，不需要安装钩子、后台服务、网络连接或仓库内密钥。

## 安全边界

| 项目 | 状态 |
|------|------|
| 默认网络访问 | 不需要 |
| 安装钩子 | 无 |
| 运行依赖 | Python 标准库脚本 |
| 密钥要求 | 不需要 |
| 写入范围 | 用户指定输出路径、发布包目录或本地 Skill 安装目录 |
| 外部平台登录态 | 仅用于用户主动发布时的临时会话，不写入仓库 |

## 密钥纪律

- 不把 ClawHub、SkillHub、GitHub token 写入仓库、README、脚本、样例、日志或发布包。
- 发布时只在命令环境、平台输入框或浏览器会话中临时使用 token。
- 包级验证会扫描 `clh_`、`skh_`、私钥片段和常见密钥文件名。
- `scripts/audit_script_safety.py` 会审计脚本中的联网导入、`shell=True`、交互输入、动态执行和密钥环境变量读取。

## 网络与外部工具

- 默认先使用包内 `docs/`、`knowledge-base/`、`assets/`、`schemas/` 和 `scripts/`。
- 浏览器、实时搜索、微博、新闻和平台后台能力只作增强。
- 没有实时访问能力时，不声称掌握当前价格、热度、排名、KOL状态或传播趋势。

## 数据安全

- 导入评论、评测字幕或规格参数前，用户应自行去除敏感个人信息、内部未发布信息和合同限制内容。
- 输出中的价格、排名、份额、新品参数和KOL近期口碑必须标注来源或 `[待验证]`。
- 上线闸门和风险判断只提供营销决策支持，不替代法务、广告合规、隐私、安全或产品审核。

## 漏洞或风险反馈

发现以下问题时，请在 GitHub issue 或私有渠道反馈：

- 发布包包含密钥、登录态或本地路径。
- 脚本出现越界写入、远程下载、隐藏网络访问或交互式阻塞输入。
- 校验脚本漏检平台拒收文件、旧版本包或不一致版本号。
- 样例、README 或平台文案承诺实时扫描、实时舆情或默认联网能力。

反馈时请提供复现命令、相关文件路径和期望结果。
